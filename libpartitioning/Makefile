THISDIR := $(realpath .)
LIBRARY = libpartitioning.so
UNAME    := $(shell uname)
CPPFLAGS = 
INCLUDES = \
	-I /usr/local/include \
	-I $(THISDIR)/../libcommon/include
LDFLAGS = \
	-L /usr/local/lib

include ../Makefile.lib
